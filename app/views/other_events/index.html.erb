

<h1>Other events</h1>

<div id="other_events">
  <% @other_events.each do |other_event| %>
    <%= render other_event %>
    <p>
      <%= link_to "Show this event", calendar_other_events_path %>
    </p>
  <% end %>
</div>

<%= link_to "New other event", new_other_event_path %>
