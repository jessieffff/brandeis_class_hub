name: Deploy

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: akhileshns/heroku-deploy@v3.12.12 # This is the action
        with:
          heroku_api_key: 5be4341e-cf0d-4cc1-9cac-0b4e22916cfc
          heroku_app_name: "rocky-hollows-83897" #Must be unique in Heroku
          heroku_email: "davidshapiro@brandeis.edu"
